#---------
# BINDING
#---------

# Map 'C-Space' as second prefix
set-option -g prefix2 C-Space

# Split panes using | and -
bind | split-window -h
bind - split-window -v

# Reload tmux config
bind r source-file ~/.config/tmux/tmux.conf \; display-message 'Reloaded Tmux!'

#------
# MISC
#------

# Start counting pane and window number at 1
set -g base-index 1
set -g pane-base-index 1

# Enable mouse control (clickable windows, panes, resizable panes)
set -g mouse on

# Don't rename windows automatically
set-option -g allow-rename off

# use vi keys in copy mode
set-option -g mode-keys vi

# Vim autoread fix
set-option -g focus-events on

# True colors
%if "#{==:#{COLORTERM},truecolor}"
set-option -a terminal-features ",*:RGB"
%endif

#--------
# DESIGN
#--------
# TODO: Make this with XDG variables
run-shell $HOME/.config/tmux/design.tmux"
